%table.table-minimal
  %tbody
    - @entries.each do |entry|
      %tr
        %td= image_tag(entry.img)
        %td= entry.name
        %td= entry.price
        %td= entry.city
        %td= entry.source
        %td= "#{time_ago_in_words entry.happend_at}前"
      %tr
        %td.content{colspan: 6}
          = raw entry.full_content

= paginate @entries
.clear
