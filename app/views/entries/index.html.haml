%table.table-minimal
  %tbody
    - @entries.each do |entry|
      %tr
        %td= entry.name
        %td= image_tag(entry.img)
        %td= entry.price
        %td= entry.city
        %td= entry.source
        %td= I18n.l entry.happend_at

= paginate @entries
.clear
