.entry{id: "#{entry.id}"}
  .entry-image
    = entry.img ? image_tag(entry.img) : image_tag("no_image.jpg")
  .entry-content
    %h1= entry.name
    %p.entry-description
      = "  #{entry.city}  #{entry.source}  #{time_ago_in_words entry.happend_at}前"
  .entry-right
    %h1= entry.price
    %p
      = link_to '原帖链接', entry.product, target: "_blank"
